# R37 Reftable Backend Evidence

R37 is accepted for the documented scope. The implementation passes local checks and scoped native
validation on Ubuntu 22.04/24.04, macOS 14 and Windows Server 2022. R16 coordinator acceptance is
the separate parent change `dd3692e22da33d6c3369010611a015665b34b713`. The executable specification
input is the published [reftable format](https://git-scm.com/docs/reftable); compatibility fixtures
must be generated independently with Git commands and original byte encoders. Upstream
implementation and test sources are excluded.

## Atomic Stages and Acceptance

1. **Record codec:** Read and write SHA-1 version 1 and SHA-256 version 2 headers, reference
   records, peeled values, symbolic values, deletion records and compressed reflog blocks. Validate
   footer CRC, block framing, restart offsets, key order, update indexes, truncation, integer
   overflow and bounded decompression. Exercise aligned and unaligned tables, indexes, log-only
   tables and empty tables from independently generated Git repositories.
1. **Stack reads:** Parse bounded `tables.list` entries without path traversal, merge references and
   reflogs by stack precedence, apply tombstones, preserve record ordering, and read coherent pinned
   generations during publication and compaction. Specify cancellation, byte, record, table and
   handle budgets; test missing/replaced tables without hidden unbounded retries.
1. **Publication and compaction:** Hold Git-compatible stack locks while checking expected values,
   publish immutable tables before replacing the list, and retain explicit outcomes at every failure
   boundary. Cover contention with Git, conditional stale writers, interrupted table/list
   publication, cleanup and compaction preserving references and retained reflogs. Compaction must
   coordinate with writers and readers without discarding concurrent updates.
1. **Reference API integration:** Select the backend from repository configuration and retain the
   existing reference-domain entry points. Cover stored and resolved edits, all expected-value
   predicates, symbolic HEAD logging, no-log updates, ref/log deletion and namespace conflicts.
   Characterize linked-worktree common/private stacks and mixed-stack transactions before deciding
   their publication contract. Unsupported required behavior needs a named queued owner.
1. **Completion evidence:** Demonstrate Git-to-girt and girt-to-Git committed-tip, reference and
   reflog observations in both formats, including a nonempty repository. Add deterministic fault and
   race cases, representative Criterion stack/read/write/compaction measurements and separate
   resource observations. Run affected native suites on Ubuntu 22.04/24.04, macOS and Windows;
   retain exact executable revisions, run URLs, artifact fingerprints and failed attempts.

Each implementation stage remains separate until its relevant checks pass. Opening a repository
alone does not establish correct import: R14's unchanged jj baseline omitted the committed reftable
tip. Final consumer integration remains R35. C02 retains existing tracing, malformed-alternate and
platform owners. R40 retains imported files-reflog interpretation; R37 must state any distinct
binary-record representation limit explicitly. Pack performance work is outside R37.

## Integration Inventory

Before R37, `References` routed reads, enumeration, reflogs, conditional writes and prepared
transactions directly to the files backend, and repository opening rejected reftable. R37 includes
prepared transactions used by colocation and preserves explicit partial effects when an operation
spans common and private stacks.

## Codec Observations

The initial codec corpus uses Git 2.55.0 on macOS arm64. Git-generated SHA-1 and SHA-256
repositories establish committed-tip, symbolic HEAD, reflog and multi-block compacted-table reads.
Git `show-ref --head`, `reflog show` and `refs verify` accept independently re-encoded tables with
matching observations. Original binary fixtures cover tombstones, peeled values, empty/log-only
tables, full unsigned timestamps, raw identity/message bytes, truncation and resource refusals.

A Git-generated reflog deletion exposed two distinctions from a literal reading of the published
format: a log-only table can locate its first block at offset zero with header-inclusive inflated
length/restart offsets, and a new transaction table can rewrite older reflog keys below its minimum
update index. The decoder accepts these observed forms. Initial failing tests remain development
history, not native acceptance evidence. Stack and transaction cases pass locally and in the scoped
native matrix.

## Implementation and Public API

The `refs::reftable` module owns original bounded table codecs, binary record types, merged stack
snapshots and explicit compaction. `RecordName` preserves uppercase pseudorefs discovered in
Git-created linked-worktree tables; ordinary reference edits retain `RefName`'s existing scope. No
dependency is added. `refs::Backend`, `Repository::reference_backend` and
`Repository::init_with_backend` expose backend selection. Existing read/list/resolve/no-log writes,
reflogs, conditional transactions and colocation use the selected backend.

`Repository::open_with_config_and_reference_limits` bounds real-HEAD configuration bootstrap;
`References::with_reftable_limits` bounds later operations. `transaction_controlled` adds
cooperative cancellation with structured partial effects. `Snapshot::read`, `Table::decode`,
`Table::encode` and `compact` provide standalone binary storage operations. The
[transaction contract](../reference-transactions.md#reftable-backend) records consistency,
resources, publication, cleanup and durability limits.

Local deterministic cases cover a Git writer excluded by held stack locks, stale expected values
after Git publication, cancellation before writing and between common/private publications, and
injected failure after immutable table creation but before list replacement. The last failure
preserves old visible records and leaves an ignored unlisted table. Native evidence is recorded
below.

Git-created linked-worktree tables contain `ORIG_HEAD`; compaction retains this binary record even
though ordinary reference operations do not expose it. Git also expects a terminal newline in a
binary reflog message. Backend append adds that delimiter; interpreted reads remove one delimiter;
the raw codec remains byte-preserving. The initial failing interoperability assertions identified
these boundaries before acceptance. No upstream implementation or test source was read.

## Remaining Owners

R40 must characterize consumer interpretation of binary reflog timestamps above `i64::MAX`,
alongside its imported files-reflog work. R37 preserves the complete unsigned field and compacts it
losslessly; `References::reflog` reports an explicit interpretation limit. R19/R20 retain
operation-specific pseudoref mutation policy; R37's codec and compaction preserve uppercase records
without extending ordinary edit names. These boundaries must remain visible at R34/R35 rather than
being treated as Git-invalid data. R38, R40, then R17 remain the next serial sequence; this task
dispatches none.

The implementation deliberately uses bounded whole-table decoding and full-stack compaction. Point
lookup decodes a stack; no near-constant lookup performance claim is made. Compaction holds writer
locks throughout its bounded work. Automatic compaction, orphan collection and log expiry are not
added; callers explicitly compact before configured stack budgets are exhausted. R31/R32 retain
maintenance planning and publication policy. No pack optimization is included.

## Exact Revisions and Validation

The executable revision is `a16ccdc7b7f178ce422abf8ab16c91ac4f600cd2`, published only through
`joshka/r37-native-validation`. Separate stages are acceptance planning
`ee1f60b722e8942524ab452b69b81374d5b26eb6`, codec `4ea217217a26ccaa05bed09b10d808e7d5dac81a` and
stack publication `04e8df4f59967cfb4feaad33c861dcf2843631cc`. Signing during publication changed
revision identities without source changes, including the supplied R16 evidence child from
`c07d9c3d2ae708c62e89f76cb95df78a5e1b0e85` to `57c4f8a082cf2d2db8fde7f6b668ae9b57849297`. Its
executable remains `60b52cabfca3cac916c642cd250711b8bec8332c`; coordinator acceptance remains
separate.

Local `just check` passes 1,682 unit cases, 1,144 integration cases and 20 doctests, formatting,
all-feature/all-target Clippy and docs.rs with warnings denied. The final linked-worktree
pseudoref-preservation assertion passes in the 24-case reftable integration corpus. Private Rustdoc
also builds with warnings denied, and rendered module and snapshot pages were visually inspected.
`actionlint` passes the executable workflow. No dependency, merge or release is included.

The first publication, `c619ec55395be549b71fb59f7d2c803cb72be300`, failed workflow parsing in run
[36172305163](https://github.com/joshka/girt/actions/runs/36172305163): a command ending in `refs::`
needed YAML quoting. Dispatch consequently returned HTTP 422. Quoting the command and running
`actionlint` corrected the workflow; no Rust source changed. Automatic index/platform runs
`36172306475` and `36172306506`, then index/reference/platform runs `36172383879`, `36172383882` and
`36172383887`, were cancelled to reuse unaffected evidence. The first scoped native run is
[36172390100](https://github.com/joshka/girt/actions/runs/36172390100).

The first scoped run passed behavioral tests on all hosts but exposed Linux's larger files-backend
preparation layout through Clippy's `large_enum_variant` diagnostic. The final executable boxes that
variant in a separate follow-up to integration revision `22f027e5b657c3fe734705f41ba3424f15c51459`.
Local scoped Clippy, 233 reference units and 72 backend/portable-reference/colocation cases pass
after the change. Existing measurements precede this files-backend allocation change; the reftable
workload and implementation are unchanged. Automatic runs `36172931224`, `36172931260` and
`36172931261` were cancelled. Final native run
[36172953299](https://github.com/joshka/girt/actions/runs/36172953299) targets the corrected
executable.

## Performance and Resources

Local measurements use Rust 1.98.1, Git 2.55.0 and macOS 26.6.2 arm64. Reports are retained under
[/Users/joshka/.codex/reports/girt-r37](/Users/joshka/.codex/reports/girt-r37).
`cargo bench --bench reftable -- --warm-up-time 1 --measurement-time 1` uses 30 Criterion samples,
SHA-1 tags and a warm filesystem cache. Publication and compaction fixture setup and teardown are
outside the measured closure. Compaction consumes the initial HEAD table and 16 update layers. These
are workload observations, not a regression comparison, cold-cache or many-reference claim.

| Operation          | Tags | Median microseconds | 95% interval         |
| ------------------ | ---- | ------------------- | -------------------- |
| Snapshot           | 16   | 91.76               | 88.87–93.10          |
| Snapshot           | 256  | 160.85              | 159.67–164.20        |
| Publish one tag    | 16   | 536.50              | 497.83–610.84        |
| Publish one tag    | 256  | 609.25              | 592.57–621.28        |
| Compact 17 tables  | 16   | 3,538.00            | 3,345.88–3,799.21    |
| Compact 17 tables  | 256  | 4,581.85            | 4,449.23–4,946.15    |

Criterion extended collection past the requested one second for some workloads to collect all 30
samples. The retained estimate JSON contains median confidence intervals; console summary estimates
use Criterion's selected estimator. The isolated resource process runs after benchmarking, with
initial HEAD and 16 updates of one tag. It reports 2,703,360 maximum resident bytes and four open
descriptors before reading, while retaining the snapshot, after compacting 17 tables and after
snapshot drop. Snapshots own decoded records and retain no open table handles. This is whole-process
peak memory for a small workload. Windows RSS and handle counts remain unmeasured.

Unix native probes reproduce the 4/4/4/4 descriptor lifecycle. Maximum resident bytes are 11,141,120
on Ubuntu 22.04, 12,742,656 on Ubuntu 24.04 and 2,441,216 on macOS 14.8.9 arm64. Python's child
resource accounting includes process launch overhead; these are whole-process peaks, not retained
snapshot allocation sizes. No filesystem type, cold-cache behavior or Windows memory/handle count is
inferred from those measurements.

## Final Native Evidence

Run [36172953299](https://github.com/joshka/girt/actions/runs/36172953299) passes at
`a16ccdc7b7f178ce422abf8ab16c91ac4f600cd2`:

| Host                | Reference units | Colocation | Portable refs | Reftable | Repository |
| ------------------- | --------------- | ---------- | ------------- | -------- | ---------- |
| Ubuntu 22.04        | 233             | 28         | 20            | 24       | 57         |
| Ubuntu 24.04        | 233             | 28         | 20            | 24       | 57         |
| macOS 14            | 233             | 28         | 20            | 24       | 56         |
| Windows Server 2022 | 229             | 28         | 24            | 24       | 56         |

Every host passes scoped Clippy and the snapshot/compaction resource probe. Git is 2.55.0 on Unix
and 2.55.0.windows.5 on Windows; Rust is 1.98.1 everywhere. Both object formats have committed-tip,
conditional reference/reflog publication and Git interoperability evidence. Native Windows tests
exercise publication and lock lifetimes; its probe reports behavior without memory or handle counts.

[Source fingerprints](r37-source.sha256) identify the final library, workflow, tests and probes.
[Artifact fingerprints](r37-artifacts.sha256) identify retained native logs and metadata, resource
JSON, local checks and Criterion estimates. Failed and cancelled attempts remain recorded above.
Final jj consumer import/export acceptance remains R35; R40, R19/R20, R31/R32 and C02 keep the
boundaries listed above. The next serial sequence remains R38, R40, then R17. No successor is
dispatched and no merge or release is performed.
