# R14 External Storage Acceptance

The coordinator accepts R14's scoped discovery and legacy-codec work and the separate
[Windows HTTP fixture repair](r14-http.md). Acceptance uses executable revision
`5efeb3d142dba943af7430f26e86fa49affc0b7b`, HTTP repair `42a724ab865976526130b3d536c6a3e82b0dcbe3`,
and evidence child `1bb48b3d8a771c3fe4ecfc1ec847c3b5f4a75c28`. Existing native storage results plus
[focused Windows HTTP verification](https://github.com/joshka/girt/actions/runs/36158949345)
establish this scoped acceptance. No replacement broad matrix is claimed.

The historical validation-blocked records below remain intact. Original unexpected HTTP errors were
not captured; the controlled preflight deadline counterexample proves the defective test assumption
without establishing those historical causes. The independently reproduced missing tracing span
remains C02-owned. R39, R15 and R16 retain alternates/extensions, bounded file-backed packs and
refresh respectively.

## Scope and Provenance

R14 characterizes A05/A07 and the retained A08/A09 questions, delivers bounded legacy pack reads,
and assigns required larger capabilities before final integration. R13 acceptance is the separate
parent change `4dd0de56`. No jj source, dependency implementation, upstream test source or
copyright-audit material was used. No dependencies or public types were added.

The unchanged consumer checkout is `be5f5ebdc200593d8f1be06f11e27b485a97093d` at
`/Users/joshka/local/jj/work/default`. Its `lib/src/git_backend.rs` and `lib/src/git.rs` establish
consumer requirements. Executable observations use installed jj 0.45.1, as earlier R10/R11 probes
do; the installed binary is not represented as a build of that exact checkout. Git 2.55.0 generates
all repository content from original strings. The host is macOS 26.6.2 arm64/APFS, Rust 1.98.1.

[The original generator](../../tests/fixtures/external-storage/observe.py) isolates Git and jj
configuration, disables signing, uses fixed Git identities/timestamps, and removes disposable
repositories. Run `python3 tests/fixtures/external-storage/observe.py` to reproduce the JSON matrix.
The [retained matrix](r14-observations.json) contains 68 observations. Every case runs in SHA-1 and
SHA-256. It records Git object reads, reflog interpretation, index entries before/after, jj
initialization, actual imported description and subsequent status. The large reflog's display is
explicitly truncated in JSON with its original byte count retained.

Pack v3 fixtures change only the documented version word and checksum in a Git-generated pack; Git
regenerates each index independently. This follows the
[pack format specification](https://git-scm.com/docs/gitformat-pack). Relative alternates are
resolved from the borrowing object directory, consistent with the
[repository layout specification](https://git-scm.com/docs/gitrepository-layout).

## Observed Acceptance Matrix

Results below agree across both hash formats. Import means the original committed description
appears in `jj log all()`, not merely successful opening. All successful imports also complete
`jj status`. These finite local observations are not native cross-platform acceptance.

| Fixture                                           | Git object read | jj import           | Current girt behavior / owner                                            |
| ------------------------------------------------- | --------------- | ------------------- | ------------------------------------------------------------------------ |
| Ordinary loose store                              | Pass            | Pass                | Supported                                                                |
| Object directory symlink                          | Pass            | Pass                | Existing support; new Unix public-API evidence                           |
| Pack v2 / index v1 or v2                          | Pass            | Pass                | Supported after R14                                                      |
| Pack v3 / index v1 or v2                          | Pass            | Pass                | Supported after R14                                                      |
| Absolute or relative alternate                    | Pass            | Pass                | Rejected at open; R39 required                                           |
| Symlinked alternate                               | Pass            | Pass                | Rejected at open; R39 required                                           |
| Alternate cycle containing requested object       | Pass            | Reject cycle        | R39 must bound traversal and define cycle outcome                        |
| Missing sole alternate containing required tip    | Missing         | Reject missing tip  | R39 must distinguish missing store/object                                |
| Missing alternate followed by valid alternate     | Pass            | Pass                | R39 must not reject the usable store globally                            |
| Split index                                       | Pass            | Pass                | R38 required                                                             |
| Sparse directory index                            | Pass            | Pass                | R38 required; opening alone is not preservation evidence                 |
| Reftable                                          | Pass            | Opens but omits tip | R37 required; baseline gap remains explicit                              |
| Intent-to-add or skip-worktree entry              | Pass            | Pass                | R13 colocation data supported; raw workflows still refuse                |
| Complete store with partialClone                  | Pass            | Pass                | R39 recognition; R26/R27 own missing-object transport decisions          |
| preciousObjects / noop                            | Pass            | Pass                | R39 recognition; R31–R33 own deletion protection                         |
| worktreeConfig                                    | Pass            | Pass                | Existing R08/R10 support                                                 |
| Unknown extension at repository version 1         | Reject          | Pass                | Keep explicit unsupported error; unknown semantics are not safe to infer |
| HTTP alternates metadata with local objects       | Pass            | Pass                | R39 must permit inert local metadata; HTTP retrieval remains R21–R27     |
| Commit graph, bitmap and multi-pack index present | Pass            | Pass                | Ordinary pack reads suffice; no accelerator requirement established      |

Girt currently permits object directory symlinks through ordinary filesystem resolution; this is not
protection against hostile replacement. R39 must preserve the external store's ownership and avoid
writing borrowed objects. R15 owns file-backed reads and memory/cache/descriptor budgets; R16 owns
refresh, publication races and retained-reader lifetimes. Neither is implemented here. Thin-pack
reception and external delta repair remain R26/R27; local stored packs in this corpus are
self-contained. R14 does not infer local thin-pack support from transport acceptance.

## Imported Reflogs and Colocation

The original fixtures include CR and NUL messages, an unterminated final record, short and suffixed
zones, seconds beyond signed 64-bit range, and a 1 MiB message. Detached-HEAD controls show Git
displaying the suffix as part of the message, retaining CR, truncating the NUL message and
displaying the overflow-date record. It emits no record for the short-zone or unterminated fixtures,
even with a ten-digit timestamp. With symbolic HEAD those cases instead display the untouched branch
reflog. These outcomes differ from some R11 fixture observations and must not be generalized into
one universal grammar rule. Exact bytes and outputs remain reproducible. jj imports and reports
status successfully for every case. These workflows do not prove that jj interprets each imported
record: production consumer inspection finds reflog construction policy, not direct reflog
iteration.

R11's strict reader remains narrower than Git, and reads the whole file without a configurable
budget. GC in R31 must consume imported logs to preserve roots; therefore R40 is required before R31
rather than dismissing these differences as unused. R40 must separate raw record iteration,
interpreted timestamps and safe append construction. It must preserve exact bytes and recoverable
object roots, bound file/record/retained bytes and cancellation, and define truncated/malformed
record recovery without silently dropping retention roots. Test these original records in both
formats, append against unusual preexisting tails, large logs, I/O faults and native platforms.
Canonical append validation must not be weakened to obtain permissive reads.

The consumer's `reset_index` builds an index from its merged parent tree and reuses stat data;
`update_intent_to_add` owns placeholders. Working-file snapshot and materialization remain jj-owned.
No additional girt raw status/checkout call requirement was found in the inspected production
consumer. R13's explicit flag editing is sufficient for that boundary. Raw flag support is not
claimed; C02 retains Windows raw-workflow assessment and R35 must repeat the call-site inventory.
R38 must test actual split/sparse reading and publication, shared-file absence/corruption, sparse
tree expansion, flags and races. Its requirement is not closed by jj's initialization succeeding.

## Delivered Behavior and Validation Contract

`Repository::objects` now reads pack v2/v3 and index v1/v2 with both object formats. The private
`pack::index` parser validates v1 framing, exact size, sorted unique identities, fanout, offsets,
coverage and checksums. Both versions share offset ordering and fanout validation. A v1 entry has no
CRC; the reader still validates pack/index checksums and reconstructed object IDs. Unknown versions
remain explicit errors. Pack writing and transport import keep their existing contracts.

Original units cover valid legacy reads, incorrect fanout/count/offset, truncation and checksum
damage. Six public-API integrations cover legacy versions with both hashes and REF/OFS deltas,
compare complete payloads against independently generated records and ask Git to read a delta. Two
Unix public-API integrations reopen symlinked object stores. Existing corruption, decode-budget and
delta-cycle cases remain applicable. No new processing algorithm or performance claim is introduced;
R15 retains meaningful file-backed resource benchmarks.

## Required Queue and Completion Boundary

Recommended serial order is R14 → R39 → R15 → R16 → R37 → R38 → R40 → R17, then the existing queue.
R39 depends on R14; R15 additionally depends on R39 so file-backed resource accounting covers all
searched stores. R37/R38 keep their existing ownership and run before C02. R40 depends on R11 and
R14 and runs before C02/R31. C02 reviews these storage changes together. R34 depends on all four
follow-ups, not just explicit refusal documentation.

R39 must deliver both-format primary/alternate loose and packed reads, relative resolution after
moves, nested and shared alternates, missing-then-valid fallback, cycle/alias bounds, symlinks and
byte/native paths. Define precedence, corrupt duplicate behavior, aggregate traversal limits,
external ownership and explicit environment policy. Visit canonical stores once: a cycle with a
reachable object must return that object as Git does; a missing-object cycle must terminate. A
missing alternate must not prevent reads satisfied by the primary or another valid store. Add
known-extension handling for noop, preciousObjects and complete partial-clone stores without
implying lazy fetching. Prove no writes or pruning in borrowed stores. Exercise Git/jj controls,
malformed path records, permissions, concurrent alternate edits, native Windows/Linux/macOS and
independently measured resource bounds. Refresh implementation stays in R16; R39 supplies its store
topology contract.

R37 must import nonempty reftable refs/reflogs and publish conditional updates with Git-readable
stack/compaction results, faults and native evidence. R38 must preserve or deliberately replace
split/sparse state through public index/colocation APIs with both-format Git observations and native
evidence. Their existing roadmap tasks remain required even where baseline behavior is incomplete.
Unknown extensions, promised-object fetch and accelerators are not silently classified as
implemented.

## Validation Record

The executable revision is `5efeb3d142dba943af7430f26e86fa49affc0b7b`, published only through
`joshka/r14-native-validation` to the authorized public origin. The first local push stopped at a
1Password signing prompt and published nothing; the retry disabled sign-on-push for that command. No
merge, release or successor dispatch occurred. The evidence child adds detached-HEAD reflog controls
and longer-date observations to the generator; library source is unchanged.

Final focused checks pass 239 pack units and 52 pack integrations (including the two Unix symlink
cases). Formatting and Markdown lint pass with 100-column prose and wider tables. The final
`just check` passes 1,612 all-feature units, 1,054 integrations and 20 doctests,
all-target/all-feature Clippy and warning-denying docs.rs. Native results and the remaining
acceptance blocker are below.

The existing Criterion warm-open workload ran with 10 samples, one-second warmup and two-second
measurement. The 16-blob fixture (3,697 pack bytes, 1,604 index bytes) measured 51.320–52.138 µs;
the 256-blob fixture (19,631 pack bytes, 8,324 index bytes) measured 87.726–89.914 µs. These are
warm filesystem measurements of pack v2/index v2 validation, not cold-storage or total-memory
bounds. Criterion compared stale local samples and printed opposite small changes for the two sizes;
this is not a controlled before/after regression assessment. R15 owns larger resource measurement.
Reproduce with:

```sh
cargo bench --bench packs -- open-validate-warm \
  --sample-size 10 --warm-up-time 1 --measurement-time 2
```

Retained local and native logs are in
[/Users/joshka/.codex/reports/girt-r14](/Users/joshka/.codex/reports/girt-r14). R14 acceptance
remains blocked by the repeated Windows HTTP failures below. The discovery matrix and bounded
storage changes are delivered; the gate is not closed.

### Native Runs

The existing [index matrix](https://github.com/joshka/girt/actions/runs/36155870070) and
[reference matrix](https://github.com/joshka/girt/actions/runs/36155870074) pass all eight jobs on
attempt 1. The [broad matrix](https://github.com/joshka/girt/actions/runs/36155870052) passes Ubuntu
22.04/24.04 and macOS 14 on attempt 1. Each Unix host passes 1,612 all-feature units, 1,582
core-only units and 20 doctests; full integrations total 1,057 per Linux host and 1,054 on macOS.
Both-format pack integrations pass 52 cases per Unix host.

The first Windows attempt fails before the broad pack/unit selections: HTTP tracing records no
`fetch.http` span for `abandoning_download_on_worker_closes_original_spans`, and three
`attempted_push_http_failures_are_uncertain_and_not_retried` cases do not return `Uncertain`. The
latter use a one-second discovery-plus-send deadline; expiration before mutation is a plausible
cause, not a verified diagnosis. Ten repeated local HTTP/tracing runs pass all 30 cases. The failed
Windows job alone is rerun at the unchanged executable revision; the original failures and skipped
steps remain retained. Separate index/reference success is not pack runtime evidence.

C02 owns investigation and stabilization of these pre-existing native HTTP/tracing tests: retain
actual structured errors on assertion failure, distinguish pre-mutation deadlines from post-send
faults with deterministic synchronization, and diagnose span capture/lifetime behavior under
parallel native execution. A passing retry must not erase the original failures or establish their
cause. R14 has changed no HTTP or tracing code.

### Windows Retry and Acceptance Blocker

Attempt 2 reruns only Windows at the same executable revision. It passes 1,430 all-feature units,
1,416 core-only units, 20 doctests, all-feature/all-target Clippy, 777 portable Git integrations
(including all 50 pack cases), 25 core/tracing and 30 HTTP/tracing integrations. The three Unix
results and eight focused matrix jobs remain successful from attempt 1. The new symlink tests are
Unix-only and are not counted as Windows evidence.

The three HTTP push fault cases fail again at `tests/http.rs:645`, with 34 other HTTP cases and all
seven portable HTTP cases passing. The assertion omits the unexpected structured error, so these
logs do not establish whether a deadline or another pre-send failure caused it. The prior tracing
failure does not recur. Examples and public Rustdoc after the HTTP step are skipped on Windows. No
third retry is launched and no HTTP implementation is changed to make this storage gate pass.

R14 remains **validation blocked**, with its scoped pack behavior demonstrated on all four native
hosts. Before accepting R14 or dispatching the next serial item, the coordinator should arrange a
bounded validation repair: expose the actual push error, make post-send fault tests wait for an
observed POST before cancellation, and rerun the Windows job. C02 retains the broader investigation
of tracing capture and native timing. This is an acceptance blocker, not a claim that R14 changed
HTTP behavior or that retry success alone diagnoses the problem.

[Source fingerprints](r14-source.sha256) identify the unchanged native-tested library, tests,
manifest and workflow. [Artifact fingerprints](r14-artifacts.sha256) identify retained logs and
observations, including both Windows attempts. All native hosts use Rust 1.98.1; Git is 2.55.0 on
Unix and 2.55.0.windows.5 on Windows. Logs retain OS and checkout/temporary filesystem details:
Ubuntu 22.04/24.04 on ext4, macOS 14.8.9 on APFS and Windows Server 2022 on NTFS.
