# R08 — Layered Configuration Resolution

R08 implements the read/resolution subset of A06. Validation is in progress; this report will retain
exact tested revisions, source fingerprints, commands, observations and remaining owners before
completion. Lossless mutation remains R09, repository layout expansion R10, transport/URL policy R21
and final jj integration R35.

See [configuration contracts](../configuration.md), the public `config` module, the executable
`examples/config.rs`, and original Git fixtures in `tests/config_resolution.rs`.
