# R12 Index Versions, Flags and Extensions

R12 implements the full-index portion of
[A09](../jj-acceptance.md#a09--index-and-colocation-primitives). R13 owns colocation and
operation-state workflows. R38 owns split shared-index storage and sparse index directory handling;
these remain explicit refusals and are required before R34.

## Acceptance Scope

- Parse and encode full v2/v3/v4 indexes in SHA-1 and SHA-256, with checked v4 path decompression.
- Retain stat words, byte paths, conflict stages, executable modes and assume-valid; add
  intent-to-add and skip-worktree data without implementing consumer staging/sparse-checkout policy.
- Preserve unchanged encoded bytes, including nonmaximal v4 compression. Bound encoded bytes,
  decompressed path bytes, entry counts and extension counts; reject corrupt/truncated framing.
- Discard stale cache trees and other derived caches after edits, retain resolve-undo bytes, and
  refuse unknown optional extensions. Preserve the prior snapshot on failed edits.
- Compare both read and write directions with independently Git-generated fixtures. Observe split
  and sparse indexes and retain their explicit unsupported boundaries.
- Preserve old publication bytes and independent concurrent changes in both hashes and new versions.
- Run local formatting, full checks, public example, private Rustdoc, Criterion and focused native
  index tests on Linux, macOS and Windows; retain exact revisions and artifacts before acceptance.

## Public Surface

`index::Version`, `Index::version`, `Index::set_version` and `IndexEdit::set_version` expose
framing. `Entry::intent_to_add` and `Entry::skip_worktree` expose extended bits. `Index::new`
selects v3 for extended flags, otherwise v2; entry edits retain the version or upgrade v2 to v3.
Explicit v2 conversion rejects extended flags. Invalid conversion leaves the old index unchanged.

The owning modules are `index::codec`, `index::entries` and `index::store`. Raw status and checkout
explicitly refuse the newly accepted flags pending caller policy. `examples/index.rs` demonstrates
SHA-256 v4 publication. No dependencies or jj consumer code are changed.

`TREE`, `UNTR`, `FSMN`, `EOIE` and `IEOT` are derived caches discarded when entries or version
change. `REUC` is retained as prior conflict information. Unknown optional extensions block edits;
unknown mandatory extensions block parsing. Identical entry replacement retains original bytes and
every extension. Payload internals remain opaque. Index equality compares
format/version/entries/extensions, not alternate encodings. Original bytes are retained alongside
decoded records, so limits describe proportional bounds rather than an exact heap budget.

## Evidence and Remaining Work

The initial implementation is `fa6afb756d6727867c3c7ac674529658b2cd3fe9`. Follow-up and final
native-test revision `0e42acf744437732604355e719e70a2eae3ead99` applies edit size limits to the
retained framing instead of first requiring a larger temporary v2 representation. Both-format
regressions cover exact-size compressed edits and failed v2 conversion without mutation. R11
acceptance is a separate parent change, `cc5fc305`, above its original evidence revision. This later
evidence child changes only documentation, including checkout/entry flag contracts; executable
behavior is unchanged. The focused native matrix passes on all four hosts; performance results
appear below. The [format specification](https://git-scm.com/docs/index-format) and Git executable
observations are the only Git implementation inputs; fixtures are original. No upstream
implementation or test source is used. Retained logs are stored under
[/Users/joshka/.codex/reports/girt-r12](/Users/joshka/.codex/reports/girt-r12).

Existing lock/publication semantics remain synchronous and per-worktree, with conservative published
mtime and no fsync/crash guarantee. Exact-byte checks detect observed noncooperating changes; they
cannot exclude an arbitrary writer after the final comparison. No staging, filter, sparse expansion,
object lookup or working-file materialization is added to the codec. The nine broader Windows
failures and R36/C02 gate remain open, as do R14 backend/imported-log and R37 reftable work.

## Git and Boundary Observations

The [retained probe](r12-observations.json) uses installed Git 2.55.0 and unchanged jj 0.45.1 in
independent temporary repositories. Both SHA-1 and SHA-256 split fixtures open in jj and report a
clean working copy. Both sparse fixtures open, but jj reports the omitted `outside/b` file as
deleted. Opening therefore does not establish sparse workflow parity. R38 retains both capabilities
before R34, including interpretation of this baseline difference. The rerunnable original probe
script is retained with the logs.

`tests/index.rs` has 50 local cases: Git stat/debug observations, flags, all three framing versions,
stages 1/2/3, executable/symlink/gitlink modes, long and non-UTF-8 byte paths, stale cache-tree
removal, Git resolve-undo interpretation after editing, linked/separate directories, lock contention
and racy stat behavior. New flag-policy tests verify raw status/checkout refusal without index or
worktree mutation on macOS/Linux. Windows excludes those four traversal cases. Git creates split and
sparse fixtures in both formats; failed girt opening preserves their bytes and releases its lock.
Git's explicit sparse expansion produces a readable full index with retained skip-worktree flags.

Focused units cover malformed/truncated v4 prefixes, overflow, unknown extended bits, decompressed
path limits, nonmaximal compression preservation, known/unknown extension policy and transactional
version refusal. Existing storage tests cover read/write/publication faults, conservative timestamp
setting and replacement-lock cleanup refusal; additional v3/v4 both-format tests inject rename
failure and an independent writer, asserting exact old or concurrent bytes and lock cleanup. No
crash-durability guarantee is added.

## Local Validation

On macOS arm64, final `just check` at `0e42acf74443` passed formatting, 1,574 all-feature units,
1,011 integration cases, 20 doctests, all-feature/all-target Clippy and warning-denying docs.rs. The
focused core filter passed 136 tests (including four existing Git-generated pack-index cases whose
names match the filter). The public SHA-256 v4 example and warning-denying private Rustdoc passed on
the initial source; the final size-limit fix has no example/Rustdoc API change. The later prose-only
checkout/entry flag contracts passed docs.rs again. Actionlint passed the focused workflow;
markdownlint and rumdl passed with the repository's 100-column prose and wider-table policy. Logs
retain initial and final runs separately; no failed attempt is counted as a pass.

## Performance

Criterion measures pure parsing and fresh encoding of 10,000 SHA-1 entries with shared directory
prefixes; fixture creation is outside the measured operation. Parsing includes retained input bytes
and owned entries; encoding measures a constructed index, not the retained-byte shortcut. Local
macOS arm64 and Rust 1.98.1 used 30 samples, 1-second warmup and 2-second measurement, with no
concurrent local build/test workload. The measured implementation is `fa6afb756d67`; the later
edit-limit correction does not change the measured parse/encode paths. The
[retained log](r12-bench.log) includes Criterion's historical comparisons, which are not used as
regression or improvement claims.

| Version | Parse estimate | Parse 95% interval | Encode estimate | Encode 95% interval |
| ------- | -------------- | ------------------ | --------------- | ------------------- |
| v2      | 2.3411 ms      | 2.3266–2.3573 ms   | 1.0123 ms       | 1.0043–1.0194 ms    |
| v3      | 2.3609 ms      | 2.3450–2.3787 ms   | 1.0629 ms       | 1.0160–1.1465 ms    |
| v4      | 2.0988 ms      | 2.0807–2.1237 ms   | 936.76 µs       | 933.23–940.35 µs    |

```sh
cargo bench --bench index -- 'index_v[234]/(parse|encode)/10000$' \
  --sample-size 30 --warm-up-time 1 --measurement-time 2 --noplot
```

No cold-I/O, allocation-budget or numerical acceptance threshold is established.

## Native Validation

The final [index matrix](https://github.com/joshka/girt/actions/runs/36126493310) passes at
`0e42acf744437732604355e719e70a2eae3ead99`. Each host runs the core index/fault filter, both-format
Git integration, the same unit filter with tracing, the SHA-256 v4 example and warning-denying
core-library/index-test/example Clippy.

| Host                       | Core/fault filter | Git integrations | Filter with tracing |
| -------------------------- | ----------------- | ---------------- | ------------------- |
| macOS 14 arm64             | 136               | 50               | 136                 |
| Ubuntu 22.04 x86_64        | 136               | 50               | 136                 |
| Ubuntu 24.04 x86_64        | 136               | 50               | 136                 |
| Windows Server 2022 x86_64 | 132               | 46               | 132                 |

The four Windows unit exclusions are Unix symlink/replaced-lock fixtures. Its integration selection
excludes the four macOS/Linux raw traversal refusal cases. Windows does execute v2/v3/v4 parsing,
flags, both-format Git interoperability, split/sparse refusal, stat preservation, lock contention,
independent-writer preservation, write/rename faults and per-worktree storage routing on NTFS. The
initial [matrix](https://github.com/joshka/girt/actions/runs/36126252797) also passes at
`fa6afb756d67`, before the two compressed-size regressions.

Retained [job results](/Users/joshka/.codex/reports/girt-r12/native-final.json),
[full logs](/Users/joshka/.codex/reports/girt-r12/native-final.log) and
[counts/environment excerpts](/Users/joshka/.codex/reports/girt-r12/native-results.log) identify
commands, compilers, Git versions and filesystems. [Source fingerprints](r12-source.sha256) cover
the final source/tests/examples/benchmarks/manifests/workflows, including the later doc-comment-only
changes. [Artifact fingerprints](r12-artifacts.sha256) cover retained local evidence and probes. No
cross-build is presented as runtime proof.

The broader [platform run](https://github.com/joshka/girt/actions/runs/36126493241) is still in
progress at this handoff and is not an acceptance result. R11's nine recorded Windows failures
remain open with R21/R36 and the R36/C02 gate. Redundant initial broad/reference runs were
cancelled; their cancellation supplies no passing evidence. The private validation bookmark remains
at the tested revision; nothing was merged or released. Coordinator acceptance of R12's scoped
completion remains pending.
