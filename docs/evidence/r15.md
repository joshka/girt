# R15 File-Backed Object Storage

The coordinator accepts R15's file-backed resource scope against
[A05](../jj-acceptance.md#a05--object-stores-resource-bounds-and-refresh). R39 coordinator
acceptance is recorded in parent change `088f92b84396f5c07e4a29a380fa799b1cd0f323`. Signing during
publication changed the local acceptance revision `86a2df6c` and R39 evidence child to
`a68dfb6dfafd3be2adb86d2f7651c34c84f37df0` without changing file contents. The original R39 evidence
identity `9c539160b06f659ae6cb92afc056e76877dfec87` remains recorded.

## Acceptance Contract

- Read primary and alternate repositories beyond the former 512 MiB whole-pack budget without
  retaining pack bytes in RAM. Preserve SHA-1/SHA-256, pack v2/v3 and index v1/v2 support.
- Preserve REF/OFS reconstruction, long-chain limits, identity checks, corruption and missing-base
  distinctions. Compare original runtime fixtures with Git; reuse unaffected import-format evidence.
- Bound aggregate validation bytes, index input and derived memory, descriptors and directory work.
  Bound per-read compressed and decoded work. Demonstrate limit failures and cleanup.
- Provide cooperative cancellation for opening and reads with explicit blocking-I/O and checkpoint
  contracts. Exercise cancellation and path-preserving I/O errors through public APIs.
- Retain reproducible Criterion open/lookup/decode observations, memory and descriptor measurements,
  and cold/warm workload descriptions without claiming a portable cold-cache guarantee.
- Run affected storage checks natively on Linux, macOS and Windows. Retain exact tested revisions
  and failed attempts alongside successful checks.

## Boundary with R16

R15 pins open pack/index file handles. Shared immutable pack state uses `Arc`; seek/read cursor
access is serialized per artifact without descriptor duplication. Files must remain immutable while
readers use them. Unlinking or renaming an open artifact follows platform semantics. This is not an
atomic pair/topology snapshot, does not protect against in-place writes, and does not provide
refresh. R16 owns publication races, external repack/GC policy, refresh, retry and retained-reader
consistency contracts. Loose reads remain live; no negative or decoded-object cache is introduced.

## Resource Policy

The default aggregate artifact-validation budget is 64 GiB on 64-bit hosts, index input is 64 MiB,
file handles are 512, pairs are 256 and pack-directory entries are 1,000,000. All stores share these
budgets. Each pair pins two descriptors; opening rejects excess rather than evicting and reopening
potentially replaced paths. Pack validation uses a 64 KiB buffer. Index IDs are read from the pinned
index by binary search; bounded entry ranges and offset-order tables remain in memory. Opening
temporarily parses one index, with input and derived allocations bounded conservatively by four
times aggregate index bytes, excluding allocator overhead and paths.

Packed reads use a 64 KiB input buffer and 8 KiB output scratch; they retain only pending delta
programs and reconstructed payloads under existing decode bounds. Compressed ranges additionally
charge a default 256 MiB per-read work budget before inflation. No cross-read cache is retained.
Caller-owned returned payloads and concurrent calls multiply memory use; this is not a process RSS
quota. Loose compressed input retains its prior output-bound policy.

Cancellation checkpoints surround alternate discovery and index parsing, occur between directory
entries and lookups, every 64 KiB of pack validation, every inflate chunk, and between delta
applications. Individual OS calls, bounded index parsing/sorting, object hashing and delta
application are synchronous and finish before the next checkpoint. Callers needing executor
responsiveness must run whole operations on bounded blocking workers and join before reclaiming
resources.

## Implementation and Initial Validation

New public calls are `Repository::objects_controlled`, `Objects::read_controlled` and
`Objects::clone`. `PackLimits` adds index-byte, descriptor and directory-entry budgets; `ReadLimits`
adds compressed-input accounting. `ObjectReadError::Cancelled` is a distinct outcome. The private
`pack::file` module owns pinned artifacts and file-backed identity lookup. Existing reconstruction
logic is shared with the in-memory test reader. No dependency or license changes occur.

The original decoder corpus now exercises file-backed reads, including missing REF bases, invalid
OFS boundaries, cycles, long chains, rejected zlib streams and resource limits. Public tests compare
all four object kinds, both formats, REF/OFS deltas and legacy versions with Git. New cases cover
exact budgets, aggregate alternate budgets, cancellation/retry, cloned concurrent readers, CRC and
checksum failures, and short reads of pinned artifacts. Unaffected import/reference/index evidence
remains applicable; the complete local suite also exercises their integration with this reader.

The [pack specification](https://git-scm.com/docs/gitformat-pack) supplies format facts.
[Original generator](../../tests/fixtures/file-storage/generate.py) asks Git 2.55.0 to create
18-object SHA-1/SHA-256 stores, each with 17 uncompressed 32 MiB blobs and one small blob. Pack
sizes are 570,469,211 and 570,469,223 bytes, respectively. The generator uses bounded chunks and
runs separately from reader measurements. No upstream implementation or test source was consulted.

Initial local `just check` found a fixture that increased pack count to 257 without increasing the
new descriptor budget; it now explicitly permits 514 handles. Earlier new truncation fixtures
exposed canonical-path assertions on macOS and Git's read-only index permissions. The fixtures now
canonicalize expected paths and recreate writable artifacts before pinning. The first unit compile
also required a default for the new read-limit field. All failed attempts are retained. Subsequent
`just check` passes 1,632 units, 1,104 integrations, 20 doctests, Clippy and docs.rs. Final native
validation and benchmark/resource observations are recorded below.

## Exact Executable and Publication

The executable is `699c1846710696115e5e7ffd1528165d5378a768`, published through
`joshka/r15-native-validation` only. The
[scoped native run](https://github.com/joshka/girt/actions/runs/36164401325) uses that exact
revision. It passes on all four native hosts. No merge, release or successor is dispatched.
Automatic push runs 36164402047 (platform), 36164401821 (references) and 36164401822 (index) were
cancelled deliberately to reuse unaffected native evidence and retain the focused matrix.

Local reports are retained under
[/Users/joshka/.codex/reports/girt-r15](/Users/joshka/.codex/reports/girt-r15). Local validation
uses Rust 1.98.1 on macOS 26.6.2 arm64/APFS. Public and private documentation builds reject
warnings. The executable fingerprint and final measurements accompany this report.

## Measurements and Reproduction

Run the generator into a disposable directory, then build and run the isolated probe:

```sh
python3 tests/fixtures/file-storage/generate.py /tmp/girt-large sha1
cargo build --release --example storage_resources
python3 tests/fixtures/file-storage/measure.py /tmp/girt-large
GIRT_STORAGE_WORKLOAD=/tmp/girt-large cargo bench --bench packs
```

The generator and probes are separate processes. The probe verifies failed-open descriptor cleanup,
opens, clones, reads twice while retaining both returned objects, drops readers/results, and checks
descriptor counts. A separate run requests cancellation after 10 ms and joins before checking
cleanup. This observes cooperative cancellation; it does not guarantee a maximum response time.

Final macOS observations use the executable above, a fresh process and warm OS file cache.
`/usr/bin/time -l` reports whole-process maximum resident bytes, not isolated allocations:

| Format  | Pack bytes  | Small-read maximum RSS | Two 32 MiB results maximum RSS | Alternate small-read RSS |
| ------- | ----------- | ---------------------- | ------------------------------ | ------------------------ |
| SHA-1   | 570,469,211 | 2,277,376              | 71,499,776                     | 2,179,072                |
| SHA-256 | 570,469,223 | 2,162,688              | 71,483,392                     | 2,179,072                |

Both formats report descriptors 4 before opening, 6 after opening, 6 after cloning and 4 after all
readers drop, including the alternate-store runs. Failed opening and cancellation return to 4. The
counter includes its own directory handle consistently. Cancellation observations are 11.297 ms and
12.762 ms from operation start, including the intentional 10 ms delay.

The [Criterion medians and 95% confidence intervals](r15-benchmarks.csv) retain the complete sampled
results in nanoseconds. On the local SHA-1 workload, warm validation of the 570 MB pack has a median
675.05 ms, small lookup 8.83 microseconds and 32 MiB decode 47.02 ms. Small Git-generated workloads
measure open, miss, ordinary and delta lookup at 16/256 blob variants. This implementation pays
filesystem calls for index identities and pack input; it makes no speedup claim over retained
memory. Earlier mixed-load and linear-sampling runs remain retained. Linear sampling exceeded its
estimated duration, so the final large workload uses 10 flat samples (20 seconds for opening, 2
seconds for reads); small workloads retain 30 samples. The one-line sampling correction is in this
evidence child, with [its own fingerprint](r15-benchmark.sha256); library code matches the native
executable. Scoped benchmark Clippy passes after the correction.

Ubuntu 24.04 reports 2,700 KiB maximum RSS for the small lookup and 68,304 KiB with both 32 MiB
results. Its cold-advised opening takes 1.377 s and reports 1,114,208 filesystem input blocks; the
following warm opening takes 0.427 s and reports zero. The Linux probe syncs and requests
`POSIX_FADV_DONTNEED` on pack/index files. This is an OS-cache experiment, not proof of physical
device-cache state. Opening validates the whole pack, so subsequent lookups already benefit from
that scan. macOS does not attempt cache eviction. Windows resource-counter limitations are recorded
with the final native results.

These fixtures emphasize large artifact size with few objects. Many-object indexes still require
bounded temporary parsing and retained offset tables; they may exhaust the separate 64 MiB index
input budget. The retained tables use two offsets plus one position per object. Allocator capacity,
paths, runtime overhead and OS caches are outside the logical limits. One transient directory or
metadata descriptor is additional to the pack-file budget. Multiple independently opened readers and
concurrent decodes multiply resources; cloned readers share pair handles and tables.

[Source fingerprints](r15-source.sha256) identify the tested library, tests, fixture generators,
probe and workflow. Neither refresh nor publication-race correctness is inferred from these resource
observations. R16 retains those contracts. C02's malformed-alternates, platform-path/ACL and tracing
owners remain unchanged, as do R37/R38/R40. Next remains R16, R37, R38, R40, then R17.

## Final Native Evidence

Run `36164401325` passes every selected job at `699c1846710696115e5e7ffd1528165d5378a768`:

| Host                           | Units | Alternates | Packs | Repository |
| ------------------------------ | ----- | ---------- | ----- | ---------- |
| Ubuntu 22.04                   | 1,632 | 35         | 63    | 57         |
| Ubuntu 24.04                   | 1,632 | 35         | 63    | 57         |
| macOS 14.8.9 arm64             | 1,632 | 33         | 63    | 56         |
| Windows Server 2022 10.0.20348 | 1,450 | 30         | 61    | 56         |

Every host also passes scoped Clippy and the original 570,469,211-byte SHA-1 fixture, including
primary/alternate small lookups, two 32 MiB results and asynchronous opening cancellation. Both
formats and pack/index versions are covered by the normal integration corpus. Large SHA-256 resource
measurements are local macOS evidence, not an additional native matrix claim. Rust is 1.98.1
everywhere; Git is 2.55.0 on Unix and 2.55.0.windows.5 on Windows.

Unix probes measure descriptor counts and whole-process RSS. Windows runs the same behavior and
cancellation probes but reports no handle count or peak RSS; those Windows measurements are not
claimed. Linux includes advisory cold-file-cache observations; neither macOS nor Windows claims a
cold-filesystem run. The workflow records OS versions and paths but does not remeasure filesystem
types; prior ext4/APFS/NTFS runner-family observations remain historical evidence.

No native attempt failed. Three unrelated automatic push matrices were deliberately cancelled as
listed above. Earlier local fixture failures and benchmark sampling warnings remain in the retained
artifacts. [Artifact fingerprints](r15-artifacts.sha256) identify local validation, benchmarks,
measurements and the complete native log and metadata. All R39 historical failures remain in its own
report and artifact manifest. Coordinator acceptance is recorded separately after evidence child
`187b9890d66021cfed5affe4d89852123141df82`; R16 owns the refresh boundary above.
