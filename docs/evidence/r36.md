# R36 Windows Native Integration Coverage

R36 inventories supported operations against
[A16](../jj-acceptance.md#a16--native-ci-and-platform-coverage), repairs the nine R08/R10 Windows
failures, and extends the existing platform workflow. C02 retains its broader storage/layout
milestone; native execution of selected cases does not establish full Windows or jj parity.

## Acceptance Criteria

- Preserve R12 acceptance as a separate documentation change above its evidence child.
- Establish Git's home-condition semantics before changing the condition matcher or its fixture.
- Rerun all nine known failures natively without disabling cases or weakening discovery assertions.
- Inventory every integration suite and distinguish omitted portable tests from unsupported
  operations, platform-specific fixtures, and future capability work.
- Execute the expanded Windows selection and applicable Linux/macOS suites, retaining exact
  revisions, logs, toolchains and filesystems.
- Keep unresolved exclusions and substantive follow-ups explicitly owned; update the roadmap and
  callback without dispatching a successor.

## R12 Acceptance and Baseline

Documentation change `da8ac831a06fcf04c3071e3aeeec523db4525da1` records coordinator acceptance above
evidence revision `145ce80c87cb03db93485be1099cae8f06c3c5a8`. The accepted executable revision
remains `0e42acf744437732604355e719e70a2eae3ead99`; R38 split/sparse implementation, R13 flag policy
and colocation, R14 characterization, R37 reftable and C02 remain open.

The existing [R12 broad run](https://github.com/joshka/girt/actions/runs/36126493241) completed at
that executable revision. Ubuntu 22.04, Ubuntu 24.04 and macOS 14 passed. Windows failed exactly the
recorded home-condition case and eight discovery assertions; its remaining selected integrations,
units and HTTP tests passed. The failed step prevented later examples and Rustdoc from executing.
Logs and job metadata are retained under
[/Users/joshka/.codex/reports/girt-r36](/Users/joshka/.codex/reports/girt-r36) as `r12-broad.log`
and `r12-broad.json`. Completed Unix jobs were inspected before launching R36 validation; the full
baseline logs were retained after its Windows job finished.

## Condition Semantics and Discovery

[Git's configuration specification](https://git-scm.com/docs/git-config#_conditional_includes)
defines `~/` expansion and trailing-slash recursive matching. The original fixture compares
`gitdir:~/repo/` with a canonical home and Git directory. On Windows, joining the suffix to a
verbatim `PathBuf` normalizes the path and drops its trailing separator. Girt then matches the
repository directory exactly instead of matching its `.git` descendant. The matcher now appends
condition bytes to the expanded home without sending glob syntax through filesystem path joining.
Include-file path expansion is unchanged.

The Git-comparison fixture checks the independent Git result before asserting girt's result.
Additional cases cover a home wildcard, an exact `.git` match and absence of recursive matching
without the trailing slash. Four unit cases exercise these same local matching boundaries without
Git. These changes preserve the expected meaning; the home case is not reclassified as a fixture
failure.

The eight discovery failures compared Git's slash-separated output bytes to Rust's canonical
verbatim/backslash spelling. They now canonicalize the Git output and compare paths, preserving
both-format discovery, ceiling and non-mutation assertions. A Unicode repository name with a
space-bearing child also compares Git with ordinary and canonical paths; on Windows the latter is a
verbatim drive path. UNC shares, WSL and denied-access ACL fixtures remain C02 evidence gaps.

## Portable Coverage and Exclusions

The canonical [suite inventory](../testing.md#native-platform-coverage) accounts for each
integration suite. No new CI matrix is added. The existing broad Windows job gains `references` and
`http`, while the R11 and R12 focused workflows remain available for ref/index faults and
both-format interoperability.

Reference enumeration and fetch-planning units no longer have whole-module Unix gates. Enumeration
symlink fixtures retain individual Unix gates. Clone completion/failure units build an in-memory
pack response instead of spawning the unsupported local adapter; their existing publication,
residual-object, cancellation and lock-failure assertions run on all hosts. Reference and clone
tracing cases likewise use the supported portable paths. No public API or dependency changes.

The `references` suite runs both formats, including loose/packed/symbolic routing, worktree/private
refs, imported/appended logs and concurrent Git writers. Its non-UTF-8 argv fixture remains
Unix-only and its raw filename fixture remains Linux-only. Packed non-UTF-8 names are separately
unit-tested without requiring those filesystem/argv capabilities.

The HTTP suite exercises SHA-1 negotiation, TLS trust and hostname checks, authentication, limits,
truncation, cancellation, partial/uncertain outcomes, ref publication and clone completion. The
shell-hook fixture remains Unix-only. SHA-256 transport negotiation remains unsupported and owned by
R26/R29; SHA-256 storage success or rejection tests are not transport parity evidence.

`fetch`, `push`, `fetch_workflow` and `clone` retain their local-process integration fixtures. The
Windows local adapter is unsupported; R25 owns native transport and R24 owns SSH lifecycle support.
R27/R28/R30 own the broader negotiated-transfer, publication and cancellation corpus as those
capabilities arrive. Portable planning/completion units and HTTP workflows now execute separately,
so the fixture exclusion does not suppress those operations entirely.

Raw status and checkout remain unsupported on Windows. Their portable suites test cancellation and
explicit refusal only. C02 must assess the consumer requirement and assign any required native
traversal/materialization implementation before R34; a green refusal test cannot close that gap.
R19/R20 retain worktree administration, ACL/registration and repair coverage; C02 retains native
UNC/drive/WSL and denied-access evidence. R31–R33 retain maintenance, R37 reftable and R38
split/sparse index storage. No network-filesystem or crash-durability guarantee is added.

## Expanded HTTP Fixture Diagnosis

The first broad [R36 run](https://github.com/joshka/girt/actions/runs/36127505314) at
`fdc0a13b8035915e7560bc0a85ab7bb4d5c7c0c2` passes all three Unix jobs and the Windows core
integration step, including all nine original regressions and the newly selected reference suite.
Its broader Windows HTTP step passes 25 cases and fails 12; Cargo's default fail-fast then skips
`http_portable`. The corrected workflow uses `--no-fail-fast` for the two HTTP suites. Canonical
Windows paths passed as `PATH_TRANSLATED` make Git's CGI backend return 404 before the intended
TLS/authentication/push scenario; `verify-pack` also rejects the verbatim absolute index filename.
Successful public HTTP fetch/clone publication uses ordinary fixture paths in that run.

The CGI fixture now starts Git inside the repository and uses a relative translated path. The
retained `probe-cgi.py` and `cgi-observations.json` in the report directory establish this spelling
on macOS; the corrected native run establishes Windows behavior. `verify-pack` likewise receives an
index path relative to its repository working directory. No library HTTP behavior or expected
cancellation/uncertainty result is changed. The reference enumeration doctest also drops its
obsolete Unix gate.

Intermediate runs `36128531559`, `36128531557` and `36128531461` at
`9ea185c19db477cc8bb041d741e5cb6359863048` were cancelled after the HTTP failure was diagnosed,
before publishing the fixture correction. Cancellation supplies no passing evidence. Initial logs
remain retained as `native-initial.log` and `native-initial.json`.

## Validation

The local `just check` passes at `9ea185c19db477cc8bb041d741e5cb6359863048` on macOS 26.6.2 arm64
with Rust 1.98.1 and Git 2.55.0: 1,578 all-feature units, 1,016 integration cases, 20 doctests,
formatting, all-target Clippy and warning-denying docs.rs. Core-only units pass 1,548 cases. Focused
config/layout/reference and HTTP runs also pass. After the HTTP fixture and doctest correction, the
affected HTTP suites, all doctests, all-target Clippy, docs.rs, formatting and actionlint pass
again. Markdownlint and rumdl pass with 100-column prose and the repository's wider-table policy.

The final [broad native run](https://github.com/joshka/girt/actions/runs/36128800142) passes all
four jobs at `f0bed7e87022282f72db1cea40da763e318ad23f`. No pending, cancelled or failed attempt is
counted as passing evidence.

| Native host                       | All-feature units | Main integrations  | Doctests |
| --------------------------------- | ----------------- | ------------------ | -------- |
| macOS 14.8.9 arm64 / APFS         | 1,578             | 1,016              | 20       |
| Ubuntu 22.04.5 x86_64 / ext4      | 1,578             | 1,019              | 20       |
| Ubuntu 24.04.5 x86_64 / ext4      | 1,578             | 1,019              | 20       |
| Windows Server 2022 x86_64 / NTFS | 1,397             | 743 core + 44 HTTP | 20       |

The Unix main integrations enable all features; Windows splits core-only and HTTP-only suites. All
selected cases pass with zero ignored or filtered tests. Platform-gated cases remain excluded as
inventoried above. Windows specifically passes all 42 config-resolution, 62 layout/shallow, 87
broad-reference, 37 broad-HTTP and seven portable-HTTP cases. The nine R08/R10 failures and all 12
newly exposed HTTP failures are resolved. Both-format Unicode discovery and ordinary/verbatim drive
spelling execute natively; this does not establish UNC or WSL behavior.

Windows also passes 1,383 core-only units, 23 tracing-only and 28 tracing/HTTP cases. All hosts pass
isolated core/HTTP/SSH library checks, tracing checks, all-feature/all-target Clippy, selected
examples and Rustdoc. Unix additionally executes its core-only, HTTP-only and SSH-only runtime
selections. SSH compilation on Windows remains compile-only evidence. All hosts use Rust 1.98.1; Git
is 2.55.0 on Unix and 2.55.0.windows.5 on Windows. Logs record filesystems for both checkout and
temporary fixture storage.

Retained `native-final.json`, `native-final.log`, `native-final-counts.json` and
`native-environment.log` in the report directory identify jobs, commands, separate feature
invocations, OS versions and results. [Artifact fingerprints](r36-artifacts.sha256) cover these and
the focused, baseline, local and probe evidence. [Source fingerprints](r36-source.sha256) cover the
final source, fixtures, tests, examples, benchmarks, manifests and workflows. Later evidence changes
are documentation-only.

The focused [reference matrix](https://github.com/joshka/girt/actions/runs/36128800110) and
[index matrix](https://github.com/joshka/girt/actions/runs/36128800162) pass on all four hosts at
that final revision. Windows runs 185 reference/fault units and 24 integrations, plus 132
index/fault filter cases and 46 integrations. Each Unix host runs 189 reference/fault units and 20
integrations, plus 136 index/fault filter cases and 50 integrations. The same filters pass with
tracing on each host; examples and scoped Clippy pass too. Filter counts overlap broader runs and
are not additive coverage totals. The 12 newly enabled Windows enumeration units explain the
reference-unit increase from R11's focused count of 173.

Performance benchmarking is inapplicable: this change preserves a configuration glob suffix and
broadens test execution without introducing a significant processing path or a performance claim.

## Completion Boundary

R36 is ready for coordinator acceptance within this portable-coverage scope. R13 resumes only after
that acceptance; this task dispatches no successor. The private validation bookmark points to the
exact final executable revision. Nothing was merged or released. C02 and the capability owners
listed above retain their explicit exclusions; R36 does not accept their future work.

## Coordinator Acceptance

The coordinator accepts R36's portable-coverage scope at executable revision
`f0bed7e87022282f72db1cea40da763e318ad23f`, with evidence child
`0b7e224506aa8f81821973581d69c87489691ca3`. The broad workflow and focused reference/index matrices
pass on all four hosts. This closes the nine R08/R10 Windows failures and twelve exposed HTTP
fixture failures and permits R13 to proceed.

C02 retains UNC/WSL/ACL/lifecycle gaps and the required Windows raw status/checkout assessment. The
capability owners documented above remain responsible for their exclusions, including R37 reftable
and R38 split/sparse index storage. This acceptance adds no merge, release, crash-durability or full
jj parity claim.
