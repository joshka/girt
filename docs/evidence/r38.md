# R38 Split and Sparse Index Evidence

R38 is ready for coordinator acceptance. R37 coordinator acceptance is the separate parent
documentation change `187760310e7d29835df20747b400efb226f88d9c`; its executable and native evidence
remain unchanged.

## Acceptance Contract

Resolve SHA-1 and SHA-256 split indexes through public index and colocation operations, including
shared-file checksums, replacement/deletion bitmaps, additions, versions and entry flags. Preserve
unchanged storage bytes; deliberately publish a standalone full index after entry or version edits.
Never modify or collect the immutable shared file. Bound aggregate main/shared input bytes and
resolved entries. Recheck both original files before publication and preserve old main-index bytes
on failure. Missing shared files require restoration; retry only after inspecting changed state.

Preserve sparse directory records and their tree identities without materializing files. Supply
explicit bounded tree expansion that retains skip-worktree policy and canonical leaf modes. Cover
missing, corrupt and wrong-kind trees, cancellation, depth, entry, byte and path limits. Sparse
storage changes must compose through colocation while leaving staging and materialization to the
caller. Successful initialization and refusal are not compatibility evidence.

Use original Git-created fixtures in both formats and independent binary boundary cases. Sources are
the published [index format](https://git-scm.com/docs/index-format) and
[EWAH format](https://git-scm.com/docs/bitmap-format); no upstream implementation or test source is
used. Required completion includes local checks, scoped native Linux/macOS/Windows execution,
publication race/fault cases, measurements and exact executable/artifact identities. R40 remains
next, then R17. R19/R20, R31/R32, R35 and existing C02 owners retain their established scope.

## Split Storage Stage

`Index::parse_with_shared` resolves explicit shared bytes; `Index::shared_index_id` identifies the
preserved dependency. Repository reads and locked edits resolve files automatically. The private
`index::split` module owns bitmap decoding and overlay resolution. `StorageError::MissingShared`
distinguishes an absent dependency. Colocation uses the same guard. No dependency is added.

Original Git CLI tests cover both-format replacements, deletions, additions, assume-valid flags,
unchanged exact-byte publication, full v4 conversion, missing/corrupt dependencies and dependency
removal during a held edit. Independent bitmap tests cover runs, literals, truncation, excessive
positions, run pointers and padding. The split stage is `4a126fcb` and passes 169 scoped unit tests,
86 index/colocation integration cases and scoped Clippy. Final native evidence is recorded below.

## Sparse Storage Stage

`Mode::SparseDirectory` names a collapsed tree with a trailing slash and skip-worktree.
`Index::expand_sparse` and `IndexEdit::expand_sparse` accept an object snapshot, `SparseLimits` and
cooperative cancellation. Tree parsing and structural validation precede leaf creation. Errors
preserve the draft; publication uses the existing locked lifecycle. Expanded leaves retain modes,
inherit assume-valid, set skip-worktree and use zero stat words. Leaf targets are not read. Unknown
optional extensions still prevent edits. No working files are created or removed.

Git-created SHA-1/SHA-256 sparse indexes round-trip exact bytes, expand to Git-readable full entries
and leave excluded directories absent. Independent tests cover leaf modes, invalid directory
flags/stages/paths, collisions, tree/output/path/byte budgets, missing/wrong-kind trees and
cancellation. Raw status/checkout keep their existing skip-worktree refusal; R14 found no additional
consumer requirement for those policy-owned operations.

## Revisions and Local Validation

The final executable is `34b5c234c06039866e6d8c8ad1c5eb732e5fa9ca`, published only through
`joshka/r38-native-validation`. The separate split stage is
`4a126fcb6e0c46c11a47c9c7eaee13c4e7c5b2c2`; sparse preservation/expansion is
`b67ee197d4c86b81e42a6f67b378187e2275de8a`; resource/fault/native validation is
`8363ce3e51ad993414560a95f1e3127c40d8c879`. No merge, release or successor dispatch is included.

`just check` passes 1,718 unit tests, 1,164 integration cases and 20 doctests,
all-target/all-feature Clippy, nightly formatting, rumdl and docs.rs. The final input-bound check
and overlapping-bitmap correction are additionally covered by 198 scoped unit cases and 100
index/colocation integration cases at the final executable. Warning-denying private Rustdoc builds.
Markdown lint passes with 100-column prose and the repository's wide-table exemption. Visual Rustdoc
inspection is unavailable: the browser URL policy rejects local file URLs. No alternate browser
route was attempted. Generated HTML contains the expected index, mode, expansion-limit and
expansion-error navigation links. No rendered-layout claim is made.

Final review used an original mutation of Git-created single-entry split indexes to give deletion
and replacement the same bit. Git accepts that representation, replacing before deleting. The
initial decoder's refusal was corrected in the final separate change; both formats now agree with
Git and preserve the original main bytes. Earlier failed compilation/style checks were corrected
locally; no failing executable is offered as acceptance evidence.

Initial automatic runs `36175755302`, `36175755162`, `36175755310` and scoped run
[36175791585](https://github.com/joshka/girt/actions/runs/36175791585) were cancelled after
discovering the bitmap-overlap case. The corrected publication's automatic runs `36175922862`,
`36175922773` and `36175922762` were cancelled to reuse unaffected evidence. Scoped native run
[36175961248](https://github.com/joshka/girt/actions/runs/36175961248) targets the final executable;
all four hosts pass. A mistyped local jj configuration flag failed before any publication; the
corrected command explicitly disabled sign-on-push, preserving source and revision identities.

## Measurements and Limits

The original `examples/index_storage_resources.rs` probe creates a disposable SHA-1 full index, asks
Git to split it, and measures complete repository reads and unchanged locked publication. It also
constructs a tree and measures sparse-draft cloning, tree expansion, output validation and
destruction. Fixture setup is outside the timed closures. Each operation reports the upper median of
30 sequential samples, with a warm filesystem cache and no confidence interval or performance gate.
Local environment is macOS 26.6.2 arm64, Rust 1.98.1 and Git 2.55.0. The isolated observations below
run after builds; the earlier concurrent-build probe remains retained separately.

| Entries | Main bytes | Shared bytes | Read median µs | Publish median µs | Expand median µs |
| ------- | ---------- | ------------ | -------------- | ----------------- | ---------------- |
| 100     | 100        | 8,032        | 50.71          | 350.04            | 53.83            |
| 10,000  | 100        | 800,032      | 1,794.83       | 2,182.08          | 3,158.96         |

Descriptors remain 4 before read, while retaining the resolved snapshot and after its drop for both
sizes. The isolated process/child maximum resident measurement is 13,516,800 bytes, including
fixture construction and Git subprocess work. It is not retained-index allocation. Windows memory
and handle counts are not measured. These observations do not establish cold-cache behavior,
large-repository scaling, a speedup or pack performance. The final bitmap-overlap correction does
not change these disjoint-bitmap workloads.

Index storage bounds aggregate main/shared encoded bytes, each parsed entry table and the resolved
entry count; retained original bytes, decoded entries and publication buffers can coexist. Bitmap
work is bounded before run expansion, and set positions cannot exceed the shared table. Sparse
limits separately bound aggregate tree occurrences, tree bytes, visited entries, generated paths and
depth; index limits bound output entries, encoded bytes and individual paths. Parsing one bounded
tree allocates its records before checking its entry count. Budgets are proportional allocation/work
bounds, not precise resident-memory or wall-clock limits. Cancellation is cooperative between
entries/trees and during supported object-read checkpoints.

Publication retains the existing index lock, timestamp, rename, cleanup and non-durable contract.
Deterministic both-format write/rename failures preserve old split/sparse bytes, release owned locks
and permit a fresh inspected retry. Main-index replacement and shared-file removal are detected;
cooperating Git writers cannot acquire the held lock. Arbitrary changes after the final byte check
remain outside the exclusion guarantee. Shared files are never created, rewritten, expired or
collected by this implementation. Full-index output after edits deliberately ends the dependency;
unchanged output preserves it and still requires the original shared file.

The R14 unchanged-jj observation and consumer inventory remain applicable: girt supplies storage and
explicit expansion while jj owns staging/materialization. R35 must repeat the final consumer
inventory and import/export acceptance. R31/R32 retain maintenance and expiry policy, R40 retains
reflog interpretation, R19/R20 retain pseudorefs, and C02 retains existing platform, tracing and
malformed-alternate owners. No new required semantic exclusion was discovered beyond those owners.

[Source fingerprints](r38-source.sha256) identify the library, integration tests, workflow and
probe. Local logs and raw measurements are retained under
[/Users/joshka/.codex/reports/girt-r38](/Users/joshka/.codex/reports/girt-r38).

## Final Native Evidence

Run [36175961248](https://github.com/joshka/girt/actions/runs/36175961248) passes at
`34b5c234c06039866e6d8c8ad1c5eb732e5fa9ca`:

| Host                | Scoped index units | Repository units | Index integration | Colocation |
| ------------------- | ------------------ | ---------------- | ----------------- | ---------- |
| Ubuntu 22.04        | 198                | 52               | 72                | 28         |
| Ubuntu 24.04        | 198                | 52               | 72                | 28         |
| macOS 14            | 198                | 52               | 72                | 28         |
| Windows Server 2022 | 194                | 47               | 68                | 28         |

Each host also passes the index units and index/colocation integrations with tracing enabled, scoped
Clippy, the public index example and the resource probe. Native Git is 2.55.0 on Unix and
2.55.0.windows.5 on Windows; Rust is 1.98.1. Platform-gated raw-workflow/symlink cases account for
count differences; split/sparse storage cases execute on all four hosts. Existing broad tracing, raw
Windows materialization and other C02 owners remain unchanged.

Native descriptors remain 6/6/6 on both Ubuntu hosts and 8/8/8 on macOS before, during and after
retaining the split snapshot at both sizes. Windows reports no handle counts. The 10,000-entry
read/publication/expansion medians in milliseconds are 3.411/2.620/3.460 on Ubuntu 22.04,
2.920/2.319/3.413 on Ubuntu 24.04, 2.550/3.194/3.994 on macOS, and 3.542/5.338/6.064 on Windows.
These shared-runner observations retain the probe's warm-cache, sample and workload limits; native
RSS is not measured. Full job logs retain filesystem/toolchain details and raw sample summaries.

[Artifact fingerprints](r38-artifacts.sha256) identify the retained local logs, native metadata,
complete job logs and resource observations. The evidence child changes documentation only.
